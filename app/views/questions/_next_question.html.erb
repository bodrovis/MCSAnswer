<div id="current-action">
  <% text = @game.next_question.present? ? 'Следующий вопрос' : 'Закончить игру' %>

  <%= link_to text, next_question_game_path(@game), class: 'btn btn-warning',
    data: {turbo_method: :patch, turbo_confirm: t('global.dialog.sure')} %>
</div>